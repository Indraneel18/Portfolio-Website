import { IconifyIcon } from "@iconify/types";
/**
 * Convert content to string, replacing IDs to make them unique
 */
declare function renderContent(content: string | IconifyIcon): string;
export { renderContent as t };